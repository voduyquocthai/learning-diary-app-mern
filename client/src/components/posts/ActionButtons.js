import Button from 'react-bootstrap/Button'

const ActionButtons = ({ url, _id }) => {
    
    
    return (<>
        <Button>
            
        </Button>
    </>);
};

export default ActionButtons;
